{
  "name": "endowcast-server",
  "version": "1.0.0",
  "description": "EndowCast backend API server",
  "main": "src/app/server.js",
  "scripts": {
    "start": "npx prisma db push --skip-generate && npx prisma generate && node src/app/server.js",
    "start:prod": "npx prisma migrate deploy && node src/app/server.js",
    "dev": "nodemon src/app/server.js",
    "build": "npx prisma generate",
    "vercel-build": "npx prisma generate",
    "postinstall": "npx prisma generate",
    "deploy": "serverless deploy",
    "migrate": "npx prisma migrate dev",
    "migrate:deploy": "npx prisma migrate deploy",
    "generate": "npx prisma generate",
    "studio": "npx prisma studio",
    "seed": "npx prisma db seed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "endowment",
    "modeling",
    "finance",
    "saas"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "@supabase/supabase-js": "^2.76.1",
    "@types/bcrypt": "^6.0.0",
    "aws-sdk": "^2.1692.0",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.11.2",
    "express-validator": "^7.2.1",
    "helmet": "^7.2.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.4",
    "stripe": "^11.17.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "prisma": "^6.17.1"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}
